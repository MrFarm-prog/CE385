// 6: ระบบตรวจสอบสิทธิ์ (รวมทุกหัวข้อ)

const VALID_USERNAME = "admin";
const VALID_PASSWORD = "ce385pass";

function login(inputUser, inputPass, role, isActive, age) {
  if (inputUser !== VALID_USERNAME || inputPass !== VALID_PASSWORD) {
    return "401: ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง";
  }

  if (isActive === false) {
    return "403: บัญชีนี้ถูกระงับการใช้งาน";
  }

  if (age < 18) {
    return "อายุไม่ถึงเกณฑ์";
  }

  if (role === "อาจารย์") {
    return "200: เข้าสู่ระบบสำเร็จ (สิทธิ์ผู้ดูแล)";
  } else if (role === "นักศึกษา") {
    return "200: เข้าสู่ระบบสำเร็จ (สิทธิ์ทั่วไป)";
  }

  return "403: ไม่พบสิทธิ์การใช้งานที่รองรับ";
}

console.log("1) สำเร็จ (อาจารย์):", login("admin", "ce385pass", "อาจารย์", true, 30));
console.log("2) สำเร็จ (นักศึกษา):", login("admin", "ce385pass", "นักศึกษา", true, 20));
console.log("3) รหัสผ่านผิด:", login("admin", "wrongpass", "นักศึกษา", true, 20));
console.log("4) ชื่อผู้ใช้ผิด:", login("wronguser", "ce385pass", "นักศึกษา", true, 20));
console.log("5) บัญชีถูกระงับ:", login("admin", "ce385pass", "นักศึกษา", false, 20));
console.log("6) อายุไม่ถึงเกณฑ์:", login("admin", "ce385pass", "นักศึกษา", true, 15));

// ส่วนที่ 3: ตอบคำถามท้ายข้อ
//
// 1. ทำไมต้องตรวจ username/password ก่อนตรวจ role
//    เพราะ role, isActive, age เป็นข้อมูลของบัญชีในระบบ ถ้ายังพิสูจน์ตัวตนไม่ได้ว่า
//    เป็นเจ้าของบัญชีจริง ก็ไม่ควรนำข้อมูลอื่นของบัญชีมาใช้ตัดสินใจอะไร
//    เพราะเท่ากับทำให้คนที่เดารหัสผ่านผิดรู้ข้อมูลบางอย่างของบัญชีคนอื่นได้ก่อน
//
// 2. ถ้าย้ายการตรวจ "อายุไม่ถึงเกณฑ์" ขึ้นไปเป็นข้อแรก จะเกิดปัญหาอะไร
//    จะทำให้คนที่ยังไม่ได้พิสูจน์ตัวตน ใส่ username/password ผิดได้ 
//    รู้ว่าบัญชีเป้าหมายมีอายุเท่าไหร่ อาจมีการรั่วไหลข้อมูลให้กับคนที่ยังไม่ผ่าน
//    การยืนยันตัวตน ซึ่งเป็นช่องโหว่ด้านความปลอดภัย