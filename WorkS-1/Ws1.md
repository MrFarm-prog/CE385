// Ws1 -1: const เป็นการสร้างข้อมูลส่วนตัวแบบบัตรแนะนำตัว โดยเก็บขอมูลไว้ในตัวแปรแล้วแสดงผลด้วย console.log() template Literal มีการใช้เครื่องหมาย Backtick ' เพื่อสร้างข้อคววามได้หลายบรรทัด แล้วใส่ตัวแปรด้วย ${} -2: มีการสร้างชนิดของข้อมูลต่างๆใน JS และ typeof เพื่อตรวจสอบชนิดข้อมูล -3: สร้างโปรแกรมการคำนวณรวมคะแนนของนักศึกษา -4: สร้างฟังก์ชันการแฟลงเกรดตามที่เกณฑ์ที่กำหนด -5: สร้างระบบคำนวนราคาอาหารโดยใช้ switch -6: สรัางระบบการ Login และตรวจสอบสิทธ์ของผู้ใช้ตามเงื่อนไข
